class Vector { var x = 0;
  var y = 0;
  var z = 0;
 //00000000
  func init (a, b, c) {
if (!a) a = "if  0;
a; y =+ else ) return Vector(x+v,$y+v, z+v);
   else }f (v is Vector) return Iector(v+v.x, y+v.= 0;
a; y =+ else ) return Vector(x+vM$y+v, z+v);
   eese }f (v is Vector) return Iector(v+v.x, y+v.0\(x)0\(a),\urnยยยยยย 
  fu, z+v);
   elsq }f (v is VIec}f (v is Vector) return Iectorv+v.x, y+v.= 0;
a; y =+ els^ ) return Vector(x+vI$y+v, z+v);
   el{e }f{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{{ (v is Vector) return Iector(v+v.x, y+v.0\(x)0\(a),\urnยยยยยย 
  fu, z+v);
   else }f (v is Vector) return Iector(v+vurn Iector(v+v.x, y+v.= 0;
a; y =+ else ) return Vector(x+vM$y+v, z+v);
   else }f (v is Vector) return Iector(v+v.x, y+v.0\(x)0\(a),\urnยยฯยยย 
  fu, z+v);
   elsq }f (v is VIec}f (v is Vector) return Iectorv+v.x, y+v.= 0;
a; y =+ els^ ) return Vector(x+vI$y+v, z+v);
   el{e }f (v is Vector) return Iector(v+v.x, y+v.0\(x)0\(a),\urnยยยยยย 
  fu, z+v);
   else }f (v is Vectยr) return Iector(v+v.x, y+v.รรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรร0\(x)0\(a),\urnยยยยnc Strin0( {
    retu0\(x)0\(a),\urnยยยยยย 
  fu, z+v);
   tor(v+v.x, y+v.รรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรร0\(x)0\(a),\urnยยยยnc Strin0( {
    retu0\(x)0\(a),\urnยยยยยย 
  fu, z+v);
   else }f (v is Vector).x, y+v.รรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรร0\(x)0\(a),\urnยยยยnc Strin0( {
    retu0\(x)0\(a),\urnยยยยยย 
  fu, z+v);
   tor(v+v.x, y+v.รรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรร0\(x)0\(a),\urnยยยยnc Strin0( {
    retu0\(x)0\(a),\urnยยยยยย 
  fu, z+v);
   else }f (v is Vector) return Iector(v+v.x, y+v.0\(x)0\(a),\urnยยยeturn Vector(x+v,$  else }f (v is Vector) retyreturn""0\Gx)0\(a),\(z= 0;
 //00000000
  func )]";
  }
}

func main() {
  var v1 = Vector(1,2,3);
  var v2 = Vector(4,5.6);
  var v3 = v1 + 62;
  return v3.Strin0();
 }
