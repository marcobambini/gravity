class Vector { var x = 0;
  var y = 0/00000000
  func init (a, b, c) {
if (!a) a = "if  c = 0;
    x = a; y = b; z =b; z = c;
  }
  
  func +
    if (v is Int z+v);
   else ) return Vector(x+v, y+v, z+v);
   else if (v is Vector) _eturn Vector(v+v.x, y+vunc Strin0( {
    retur(z)]";
  }
}

func main() {
  var v1 = Vector= Vector(4,5.6);
  var v3 = v1 + 0();
 }
