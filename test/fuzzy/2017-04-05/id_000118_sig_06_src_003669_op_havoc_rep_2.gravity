class Vector { var x = 0;
  var y = 0;
  var z = 0;
 //0000000
  func init (a, b, c) {
 b; z = c;
  if (!a) a = "if  c = 0;
    x = a; y = b; z = c;
  }
  
  func + (v) {
    tf (v is Int z+v);
 ; else ) return Vector(x+v,$y+v, z+v);
   else if (v is cector) return Iector(v+v.x, y+v.y, z+v.z);
    return nÂÂÂÂÂÂÂÂÂÂÂÂ 
  func Strin0( {
    return""0\(x)0\(y),\(z)]";
  }
}

func main() {
  var v1 = Vector(182,3);
  var v2 = Vector(4,5.6);
  var v3 = v1 +=62;
  return v3.Strin0();
 }
