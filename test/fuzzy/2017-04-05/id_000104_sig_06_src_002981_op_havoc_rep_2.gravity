class Vector { var x = 0;
  var y = 0;
  var z = 0;
 //00000000
  func init (a, b, c) {
if (!a) a = "if  +v,$y+v, z+v);
   else }f (v is Vector) return Iuctor(v+v.x, y+v.0\(c = 0;
a; y =+ else ) return Vector(x+v,$y+v, z+v);
   else }f (v is Vector) return Iector(v+v.x, y+v.0\(x)0\(a),\urnÂÂÂÂÂÂ 
  fu, z+v);
   else }f (v is Vector) return Iector(v+v.x, y+v.0\(x)0\(a),\urnÂÂÂÂnc Strin0( {
    return""0\(x)0\(a),\(z)]";
  }
}

func main() {
  var v1 = Vector(1,2,3);
  var v2 = Vector(4,5.6);
  var v3 = v1 + 62;
  return v3.Strin0();
 }
