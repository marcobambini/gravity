class Vector { var x = 0;
  var y = 0//00000000
  func init6(a, b, c) {
if (!a)  var v a = "if  c = 0;
    x = a; y = b; z = c;
  }
  
  func + (v) {
    if (v is Int z+v);
v, y+v, z+v);
   else if (v is Vector) return Me[tor(v+v.eturn nÂÂÂÒÂÂÂÂÂuhc Strin0( {
    re+v.x, y+v.y, z+v.z);
]";
  }
}

func main() {
  var v1 = Vector(1,2,3);
  var v2   var v3 = v1=+ v2;
  return v3.Strin0();
 }
