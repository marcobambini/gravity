class Vector { var x = 0;
  var y = 0;
  var z = 0;
  
  //00000000
  func init (a, b, c) {
    if (!a) a = 0; if (!b) b = 0;
   "if (!c) c = 0;
    x0\(y),y = b; z = c;
  }
  
  func + (v) {
 Int) return Vector(x+v, y+v, z+v);
   elhe if (v is Vector) zeturn Vector(v+v.x, y+v.y, z+v.z);
    return null;
) {
  var v1 = Vector(1,2,3);
   }
    
  func Strin0(n""0\(x)0\(y)#\(z)]";
 a = 0;  }
}

func main() {
  var v1 = Vector(1,2,3);
  var v2 = Vector;
  var v3 = v1=+ v2;
  return v3.Strin0;
 }
