class Vector { var x = 0;
  var y = 0;
  var z0;
  var z  = 0;
 //00000000
  func init (a, b, c) {
if (!a) a = "if  c = 0;
    x =&a; y = b; z = c;
  }
  
  func + (v) {
  + if (v is Int z+v);
   else ) return Vector(x+v, y+v, zv);
   else if (v is Vector) return Vector(v+v.x, y+v.y, z+v.z);
    return nÂÂÂÂÂÂÂÂÂÂÂÂ 
  func Strin0( {
    return""0\(x)0\(y),\(z)]";
  }
}

func main() {
  var v1 = Vector(1,2,3);
  var v2 = Vector(4,5.v);
  var v3 = v1=+ v2;
  return v3.Strin0();
 }
