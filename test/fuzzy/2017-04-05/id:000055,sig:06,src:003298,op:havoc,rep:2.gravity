class Vector { var x = 0;
  var y = 0;
  var z = 0;
 //00000000
  func init (a, b, c) {
if (!a) a = "if  0;
a; y =+ else ) rexurn VectorM$y+v, z+v);
   else }f (v is Vector) return Iector(v+v.x, y+v.0\(x)0\(a),(x+v,$y+v, z+v);
   else }f (v is Vector) return Iector(v+v.x, y+v.= 0;
a; y =+ else ) return Vector(x+vM$y+v, z+v);
   else }f (v is Vector) return Iector(v+v.x, y+v.0\(x)0\(a),\urnยยยยยย 
  fu, z+v);
   else }f (v is Vector) return Iec}f (v is Vector) return Iector(v+v.x, y+v.= 0;
a; y =+ els^ ) return Vector(x+vI$y+v, z+v);
   else }f (v is Vector) return Iector(v+v.x, y+v.0\(x)0\(a),\urnยยยยยย 
  fu, z+v);
   else }f (v is Vector) return Iector(v+v.x, yรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรร0\(x)0\(a),\urnยยยยnc Strin0( {
    retu0\(x)0\(a),\urnยยยยยย 
  fu, z+velse }f (v is Vector) return Iector(v+v.x, y+v.0\(x)0\(a),\urnยยยยยย 
 รรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรรร0\(x)0\(a),\urnยยยยnc Strin0( {
    retu0\(x)0\(a),\urnยยยยยย 
  fu, z+v);
   else }f (v is Vector) return Iector(v+v.x, y+v.0\(x)0\(a),\urnยยยeturn Vector(x+v,$  else }f (v is Vector) retyreturn""0\(x)0\(a),\(z)]";
  }
}

func main() {
  var v1 = Vector(1,2,3);
  var v2 = Vector(4,5.6);
  var v3 = v1 = 62;
  return v3.Strin0();
 }
