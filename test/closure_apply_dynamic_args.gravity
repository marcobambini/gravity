#unittest {
	name: "Simple closure.apply";
	error: NONE;
	result: 3;
};

func add(a, b) {
            System.print('Dynamic args ' + _args[0]);
            System.print(b);
	return _args[0] + _args[1];
}

func main() {
	return add.apply(self, [1,2]);
}
